fastapi>=0.115.0
uvicorn[standard]>=0.52.4
python-multipart>=0.0.12
pillow>=10.0.0
numpy>=1.24,<2
imagehash>=4.3.1
scipy>=1.11,<1.14
# Windows-stable OCR stack (PaddleOCR 3.x hits oneDNN PIR bugs on Win):
paddlepaddle==2.6.2
paddleocr==2.7.3
# Tests (HTTP razina; CI ih instalira zasebno):
httpx>=0.27

# Optional CLIP band match (large):
# torch
# open-clip-torch
