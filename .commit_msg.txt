feat(W3): price freshness — fetchedAt schema, UI note, 90-day stale warning

Schema:
- fetchedAt (YYYY-MM-DD) on Vitola, Vitola.regionLinks values, and
  Cigar.regionLinks values in app/src/types.ts
- ResolvedPrice interface exported from data/index.ts; cigarPriceForMarket
  now returns fetchedAt; new cigarLatestFetchedAt helper

i18n:
- Added price.snapshotNote ("Cijena preuzeta {date}." / "Price as of {date}.")
- Added price.staleNote (90-day stale warning, HR + EN)
- Existing price.marketNote unchanged

UI:
- CigarPriceNote in DetailSheet: shows "Cijena preuzeta {date}." when
  fetchedAt present, orange stale warning when > 90 days, fallback to
  generic marketNote when fetchedAt absent

Tests (10 new, all green):
- YYYY-MM-DD format validation on vitolas and regionLinks
- No fetchedAt in future
- Resolver passthrough for EU/USA/HR and undefined when absent

Scripts (offline — no scrapes run):
- shop_common.py: rates updated to mid-2026 values (USD 0.91, GBP 1.19,
  CHF 1.06) with date comment and FETCH_DATE constant
- enrich-region-links.py: stamps fetchedAt=FETCH_DATE on each link entry
- sync-hr-shops.py: stamps fetchedAt=today on vitola writes and upserts

New offline tools:
- stamp-fetched-at-baseline.py: one-shot script that sets fetchedAt=2026-07-01
  on all existing price points without network access

AGENTS.md: documented quarterly price refresh cadence and script usage
