# Archived one-shot scripts

Moved here during audit remediation because they were underscore-prefixed
local helpers not referenced by CI, `pipeline.py`, or docs. Kept for history;
do not import from app runtime paths.
