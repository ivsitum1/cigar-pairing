{
  "skill_id": "memory-decision-quality",
  "version": "1.0",
  "description": "AutoMem AM-7: eval seed for promoting good memory decisions from trajectories (non-LoRA)",
  "cases": [
    {
      "id": "case_promote_via_bridge",
      "input": {
        "text": "After a successful fold-lemma, promote the memory decision to solved_lemmas without fine-tuning."
      },
      "assertions": [
        {
          "type": "regex_match",
          "value": "(?i)(memory_trace_bridge|solved_lemmas|promote|trajectory)",
          "description": "references trajectory promotion bridge"
        }
      ]
    },
    {
      "id": "case_no_lora",
      "input": {
        "text": "How does agent-rules implement AutoMem proficiency loop?"
      },
      "assertions": [
        {
          "type": "regex_match",
          "value": "(?i)(solved_lemmas|verbatim|trace|reject.*lora|no.*fine.?tun)",
          "description": "harness trace promotion, not weight training"
        }
      ]
    },
    {
      "id": "case_memory_op_vocabulary",
      "input": {
        "text": "Which memory operations are first-class in the harness?"
      },
      "assertions": [
        {
          "type": "regex_match",
          "value": "(?i)(log|read|write|plan)",
          "description": "AutoMem file-op vocabulary"
        }
      ]
    }
  ]
}
